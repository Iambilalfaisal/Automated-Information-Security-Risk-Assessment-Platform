# Streamlit UI (primary) — used by Streamlit Cloud and local: streamlit run app.py
streamlit>=1.28.0
plotly>=5.18.0
pandas>=2.0.0

# Backend / risk engine (shared with streamlit_lib)
python-dotenv>=1.0.0
requests>=2.31.0
anthropic>=0.39.0
reportlab>=4.0.0
python-docx>=1.1.0
matplotlib>=3.8.0

# Optional: Flask REST API (legacy / testing)
flask>=3.0.0
flask-cors>=4.0.0

# Development
pytest>=8.0.0
